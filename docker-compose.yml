version: "3.8"

services:
  mongo:
    image: mongo:8.2.5
    container_name: mongo
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data: